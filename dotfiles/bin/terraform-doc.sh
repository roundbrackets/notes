#!/bin/bash

file=TERRAFORM_DOCS.md
terraform-docs md ./ | cat -s | ghead -n -1 > ./$file
echo >> ./$file
str="$file generated by `terraform-docs --version` on `date`"
echo "${str}"
echo "_${str}_" >> ./$file
